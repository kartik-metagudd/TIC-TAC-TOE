# REQUIRMENTS

1) Make sure u have/installed  'g++' compiler,

goto "https://sourceforge.net/projects/mingw/" download 'Mingw' and select g++ compiler while install,

add the lib and bin file path to environmental variables path , if still don't get it, watch a video on how to do it.

2) 'git' download git from "https://git-scm.com/downloads" and restart ur system once.


# Tic-Tac-Toe Game

This is a simple Tic-Tac-Toe game implemented in C++. It allows for single-player and two-player modes.

## Getting Started

### 1. Clone the Repository

If you haven't already, clone this repository to your local machine:

```bash
git clone https://github.com/kartik-metagudd/TIC-TAC-TOE.git
cd tic-tac-toe

### 2. Running the Game

After cloning the repository, you can run the game using the executable provided.

#### For Windows

Navigate to the directory where the `3t.exe` file is located and run:

command line (cmd)
run the command - 3t.exe or just 3t

for linux 
run the command - ./3t
